export { getDocuments, addDocument, deleteDocument } from './store';
export const updateDocument = (profileId: number, docId: number, patch: any) => {
  const { getDocuments } = require('./store');
  // Simple update stub
};
